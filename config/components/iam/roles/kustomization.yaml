# This kustomization program is used to create all of the Milo IAM roles that
# are available to users.
#
# Each role has a corresponding configuration file in this directory.

resources:
  - ipam-viewer.yaml
  - ipam-consumer.yaml
  - ipam-provider.yaml
  - ipam-admin.yaml
