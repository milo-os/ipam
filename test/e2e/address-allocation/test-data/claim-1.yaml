apiVersion: ipam.miloapis.com/v1alpha1
kind: IPAddressClaim
metadata:
  name: addr-claim-1
  namespace: ($namespace)
  labels:
    addr-test: "true"
spec:
  ipFamily: IPv4
  prefixRef:
    name: addr-pool
  reclaimPolicy: Delete
